# 桩模块审计报告（已归档）

> ⚠️ **本报告为 2026-05-22 历史记录。当前系统已无批量桩模块问题。**
>
> 当前状态：
> - **总模块**: 457
> - **真桩模块**: 1（`system_coordinator.py` 遗留 shim，已被 v3 包替代）
> - **检测方式**: 从纯文件大小阈值（<2KB）升级为 `_is_true_stub_file()`（检查是否有真实 `def` 方法体）
>
> 历史记录保留以追踪改进路径。

## 历史数据（2026-05-22）

| 指标 | 当时值 | 当前值 |
|------|--------|--------|
| 总模块数 | 535 | 457 |
| 桩模块 (<2KB) | 95 (17%) | N/A — 改用真实逻辑检测 |
| 真桩模块 | 95 | **1 (0.2%)** |

## 修复记录

2026-05-31: 修复 `infra.py` 中的 `get_stub_count()`/`get_stubs()` 方法，
改用 `_is_true_stub_file()` 检测是否有真实 `def` 方法（排除仅 `__init__`）。
结果：stubs 从 95 → 1。

### 曾被误标为桩但实际有真实逻辑的文件

- `batch_processor.py` (1.4KB) — 含 `Pool.map` 多进程并发批处理
- `sms_gateway.py` (1.9KB) — 含阿里云/腾讯云短信通道实现
