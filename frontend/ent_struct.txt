
=== Around </script> at 3335 (lines 3325-3345) ===
        function resetToDefault() {

            pendingAction = { type: 'reset' };

            document.getElementById('confirmMessage').textContent = '确定要重置所有模块到默认状态吗？';

            document.getElementById('confirmModal').classList.add('show');

        }

    </script>

<!-- AUTO-EVO-AI V0.1 API Bridge -->
<!-- evo-api-bridge.js 已内联 -->
<script>
// 系统状态监控面板集成
(function() {
    // 异步加载后端模块详细信息
    async function loadBackendModuleInfo(moduleId) {
        const area = document.getElementById('backend-info-area');
        if (!area || !EvoAPI.isConnected()) return;

=== Around <script> at 3339 (lines 3329-3349) ===
            document.getElementById('confirmMessage').textContent = '确定要重置所有模块到默认状态吗？';

            document.getElementById('confirmModal').classList.add('show');

        }

    </script>

<!-- AUTO-EVO-AI V0.1 API Bridge -->
<!-- evo-api-bridge.js 已内联 -->
<script>
// 系统状态监控面板集成
(function() {
    // 异步加载后端模块详细信息
    async function loadBackendModuleInfo(moduleId) {
        const area = document.getElementById('backend-info-area');
        if (!area || !EvoAPI.isConnected()) return;
        // 前端用kebab-case，后端用snake_case
        const backendName = moduleId.replace(/-/g, '_');
        try {
            const [info, codeInfo] = await Promise.all([

=== Around </script> at 6996 (lines 6986-7006) ===
    window._exToggleLogAuto = function(on) {
        if (_explorerState.logAutoRefresh) { clearInterval(_explorerState.logAutoRefresh); _explorerState.logAutoRefresh = null; }
        if (on) { _explorerState.logAutoRefresh = setInterval(() => _exRefreshLog(), 3000); }
    };

    function escHtml(s) {
        if (s == null) return '';
        return String(s).replace(/&/g,'&amp;').replace(/</g,'&lt;').replace(/>/g,'&gt;').replace(/"/g,'&quot;');
    }
})();
</script>


    <!-- Mobile Bottom Navigation -->
    <nav class="mobile-nav" id="mobile-nav">
        <div class="mobile-nav-item active" onclick="mobileNavTo('home')">
            <span class="mobile-nav-icon">🏠</span>
            <span>首页</span>
        </div>
        <div class="mobile-nav-item" onclick="mobileNavTo('modules')">
            <span class="mobile-nav-icon">📦</span>

=== Around <script> at 7023 (lines 7013-7033) ===
        <div class="mobile-nav-item" onclick="mobileNavTo('monitor')">
            <span class="mobile-nav-icon">📊</span>
            <span>监控</span>
        </div>
        <div class="mobile-nav-item" onclick="mobileNavTo('docs')">
            <span class="mobile-nav-icon">📖</span>
            <span>文档</span>
        </div>
    </nav>

    <script>
    function mobileNavTo(section) {
        document.querySelectorAll('.mobile-nav-item').forEach(i => i.classList.remove('active'));
        event.currentTarget.classList.add('active');
        switch(section) {
            case 'home': window.scrollTo({top:0,behavior:'smooth'}); break;
            case 'modules': document.getElementById('section-modules')?.scrollIntoView({behavior:'smooth'}); break;
            case 'coord': openV3Panel(); break;
            case 'monitor': openMonitorPanel(); break;
            case 'docs': window.open('/docs','_blank'); break;
        }

=== Around </script> at 7039 (lines 7029-7049) ===
            case 'modules': document.getElementById('section-modules')?.scrollIntoView({behavior:'smooth'}); break;
            case 'coord': openV3Panel(); break;
            case 'monitor': openMonitorPanel(); break;
            case 'docs': window.open('/docs','_blank'); break;
        }
    }
    // Detect mobile
    if (/Android|iPhone|iPad|iPod|Mobile/i.test(navigator.userAgent)) {
        document.body.classList.add('mobile-device');
    }
    </script>

<!-- ═══════════════════════════════════════════════════════════════
     SETUP WIZARD — 首次配置向导
     用户输入API Key/Token等凭证后自动保存到后端config_center
     ═══════════════════════════════════════════════════════════════ -->
<div id="setup-wizard-overlay" style="display:none">
  <div id="setup-wizard">
    <div class="sw-header">
      <div class="sw-logo">⚡</div>
      <h2 class="sw-title">系统配置向导</h2>

=== Around <script> at 7125 (lines 7115-7135) ===
.sw-complete-icon{font-size:64px;text-align:center}
.sw-complete-title{font-size:20px;font-weight:700;color:#4ade80;text-align:center;margin:10px 0 6px}
.sw-complete-desc{font-size:13px;color:#94a3b8;text-align:center;line-height:1.7;margin:0}
.sw-complete-stats{display:grid;grid-template-columns:repeat(3,1fr);gap:10px;margin:20px 0}
.sw-cs-item{text-align:center;padding:12px;background:rgba(255,255,255,.03);border-radius:10px}
.sw-cs-item .scs-num{font-size:24px;font-weight:700;color:#818cf8}
.sw-cs-item .scs-label{font-size:11px;color:#64748b;margin-top:2px}
@media(max-width:500px){.sw-welcome-features,.sw-complete-stats{grid-template-columns:1fr}#setup-wizard{border-radius:16px}}
</style>

<script>
/* ─── SETUP WIZARD LOGIC ─── */
const SW_STEPS = ['welcome', 'ai', 'notify', 'schedule', 'complete'];
let swStep = 0;
let swData = {};
let swChecked = {};

/* 检测是否应该显示向导 */
function checkSetupWizard() {
  fetch('/api/config/items?group=llm').then(r => r.json()).then(data => {
    const items = data.items || [];

=== Around </script> at 7469 (lines 7459-7479) ===
  ];
  for (const t of tasks) {
    if (!toggles[t.key]) {
      try { await fetch('/api/scheduler/tasks/' + t.id + '/toggle', { method: 'POST' }); } catch(e) {}
    }
  }
}

/* Auto-show on load */
setTimeout(checkSetupWizard, 1500);
</script>

// BGOS桩函数（原型缺失的部分）
function createDepartmentFromModal() { showToast('部门创建功能开发中...', 'info'); }
function deleteApproval(id) { showToast('删除审批功能开发中...', 'info'); }
function renderCompanies() { document.getElementById('page-companies'); (document.getElementById('page-companies').innerHTML = '<p>公司管理（开发中）</p>'); }
function renderDepartments() { document.getElementById('page-organization'); (document.getElementById('page-organization').innerHTML = '<p>组织架构（开发中）</p>'); }
function renderTasks() { document.getElementById('page-tasks'); (document.getElementById('page-tasks').innerHTML = '<p>任务列表（开发中）</p>'); }
function renderFinance() { document.getElementById('page-finance'); (document.getElementById('page-finance').innerHTML = '<p>财务数据（开发中）</p>'); }
function renderDashboard() { updateDashboard(); }
function renderOrganization() { renderDepartments(); }

=== Around <script> at 7542 (lines 7532-7552) ===
    padding: 12px 20px; border-radius: 12px; max-width: 360px;
    background: rgba(15,23,42,0.95); backdrop-filter: blur(20px);
    border: 1px solid rgba(99,102,241,0.3); color: #e2e8f0;
    font-size: 13px; box-shadow: 0 8px 32px rgba(0,0,0,0.3);
    transform: translateX(120%); transition: transform 0.4s cubic-bezier(0.16,1,0.3,1);
}
.ws-toast.show { transform: translateX(0); }
.ws-toast-title { font-weight: 600; margin-bottom: 4px; color: #c4b5fd; }
</style>

<script>
// ═══════════════════════════════════════════════════════
// i18n 国际化系统
// ═══════════════════════════════════════════════════════
const I18N = {
    current: localStorage.getItem('evo-locale') || 'zh',
    translations: {
        zh: {
            // 侧边栏
            'nav.dashboard': '监控面板', 'nav.agents': 'Agent管理', 'nav.workflows': '工作流编排',
            'nav.tasks': '任务中心', 'nav.memory': '记忆管理', 'nav.evolution': '进化中心',

=== Around <script> at 9195 (lines 9185-9210) ===
    </div>
    <div class="modal-body" id="modal-body">
      <!-- 动态内容 -->
    </div>
  </div>
</div>

<!-- ═══════════════════════════════════════════════════════════════
     JavaScript 核心引擎
     ═══════════════════════════════════════════════════════════════ -->
<script>
// ═══════════════════════════════════════════════════════════════
// 全局状态
// ═══════════════════════════════════════════════════════════════
const BGOS = {
  version: '6.0',
  theme: localStorage.getItem('bgos-theme') || 'dark',
  autopilot: false,
  currentUser: { id: 'u-001', name: '董事长', role: 'super-admin', type: 'human' },
  groups: [], companies: [], departments: [],
  users: [{ id: 'u-001', name: '董事长', account: 'founder', role: 'super-admin', companyId: null, deptId: null, status: 'active', type: 'human' }],
  agents: [], tasks: [], approvals: [], finances: [], auditLogs: [],
  nextId: { group: 1, company: 1, dept: 1, user: 2, agent: 1, task: 1, approval: 1 }
};

const INDUSTRY_TEMPLATES = {

=== Before </script> at 9943 (lines 9900-9955) ===
  // 智能体集群
  setText('agent-total', BGOS.agents.length.toLocaleString());
  setText('agent-running', BGOS.agents.filter(a => a.status === 'running').length.toLocaleString());
  setText('agent-sleeping', BGOS.agents.filter(a => a.status === 'sleeping').length.toLocaleString());
  setText('agent-tasks-today', BGOS.tasks.filter(t => t.status === 'running').length);

  // 任务
  setText('task-pending', BGOS.tasks.filter(t => t.status === 'pending').length);
  setText('task-running', BGOS.tasks.filter(t => t.status === 'running').length);
  setText('task-finished', BGOS.tasks.filter(t => t.status === 'finish').length);
  setText('task-rejected', BGOS.tasks.filter(t => t.status === 'reject').length);

  // 财务
  const totalBudget = BGOS.companies.reduce((s,c) => s + (c.budget || 0), 0);
  const totalIncome = BGOS.companies.reduce((s,c) => s + (c.income || 0), 0);
  const totalProfit = BGOS.companies.reduce((s,c) => s + (c.profit || 0), 0);
  setText('finance-income', '$' + totalIncome.toLocaleString());
  setText('finance-cost', '$' + (totalIncome - totalProfit).toLocaleString());
  setText('finance-profit', '$' + totalProfit.toLocaleString());
  setText('finance-cash', '$' + totalBudget.toLocaleString());
  setText('finance-budget', '$' + totalBudget.toLocaleString());
  setText('finance-valuation', '$' + totalValuation.toLocaleString());

  // 表格
  renderAgentTable();
  renderCompanyTable();
  renderDeptTable();
  renderHumanTable();
  renderAgentMgmtTable();
  renderTaskTable();
  renderApprovalTable();
  renderAuditTable();
}

function setText(id, text) {
  const el = document.getElementById(id);
  if (el) el.textContent = text;
}

// ═══════════════════════════════════════════════════════════════
// 启动
// ═══════════════════════════════════════════════════════════════
document.addEventListener('DOMContentLoaded', init);
</script>

`;
    // Execute BGOS JS
    var script = document.createElement('script');
    script.textContent = `// ═══════════════════════════════════════════════════════════════
// 全局状态
// ═══════════════════════════════════════════════════════════════
const BGOS = {
  version: '6.0',
  theme: localStorage.getItem('bgos-theme') || 'dark',
  autopilot: false,
  currentUser: { id: 'u-001', name: '董事长', role: 'super-admin', type: 'human' },

=== Around </script> inside template at 10700 (lines 10690-10710) ===

// ═══════════════════════════════════════════════════════════════
// 启动
// ═══════════════════════════════════════════════════════════════
document.addEventListener('DOMContentLoaded', init);`;
    container.appendChild(script);
    // Also re-run init after DOM render
    setTimeout(function() {
        if (typeof init === 'function') init();
    }, 100);
}</script>`;
    container.appendChild(closeBtn);
    overlay.appendChild(container);
    document.body.appendChild(overlay);
    
    // Now inject and execute the BGOS script
    var script = document.createElement('script');
    script.textContent = `(function() {
        
// ═══════════════════════════════════════════════════════════════
// 全局状态

=== Around </script> at 12309 (lines 12299-12320) ===
window.toggleAutopilot = toggleAutopilot;
window.toggleGroup = toggleGroup;
window.toggleSidebar = toggleSidebar;
window.toggleTheme = toggleTheme;
window.wakeAllAgents = wakeAllAgents;
window.showBillionOSModal = showBillionOSModal;

// === auto-generated window exports for onclick ===
;

</script>

</html>


