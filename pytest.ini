[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --tb=short --strict-markers --cov=api --cov-report=term
asyncio_mode = auto
markers =
    slow: 慢测试，需要较长时间
    e2e: 端到端测试
    unit: 单元测试
    integration: 集成测试
    compliance: 模块合规测试
